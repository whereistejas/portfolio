[package]
name = "preview"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
axum = "0.8"
flate2 = "1"
mime_guess = "2"
percent-encoding = "2"
tokio = { version = "1", features = ["fs", "macros", "net", "rt-multi-thread"] }

[package]
name = "prerender"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
leptos = { version = "0.8", features = ["ssr"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
site = { path = "../site" }

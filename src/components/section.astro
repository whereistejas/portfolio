---
interface Props {
  key: string;
}

const { key } = Astro.props;
---

<div class="grid grid-cols-1 sm:grid-cols-[2fr,3fr] justify-items-start">
  <p>{key}</p>
  <div class="grid grid-cols-1">
    <slot />
  </div>
</div>

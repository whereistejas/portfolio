---
import { ClientRouter } from "astro:transitions";
import PostHog from "../components/posthog.astro";

import "../styles/global.css";

const { flex_start = true, title, latex = false } = Astro.props;
---

<html lang="en">
  <head>
    <PostHog />
    <!-- Like to view source? We might get along email@whereistejas.xyz -->

    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <a rel="me" href="https://hachyderm.io/@whereistejas" hidden>Mastodon</a>

    {
      latex && (
        <link
          rel="stylesheet"
          href="https://cdn.jsdelivr.net/npm/katex@0.15.2/dist/katex.min.css"
          integrity="sha384-MlJdn/WNKDGXveldHDdyRP1R4CTHr3FeuDNfhsLPYrq2t0UBkUdK2jyTnXPEK1NQ"
          crossorigin="anonymous"
        />
      )
    }

    <title>{title === "" ? "Tejas Sanap" : `${title} | Tejas Sanap`}</title>

    <ClientRouter />
  </head>
  <body style={flex_start ? "justify-content: flex-start;" : ""}>
    <header>
      <slot name="header" />
    </header>

    <main>
      <slot name="main" />
    </main>

    <footer>
      <slot name="footer" />
    </footer>
  </body>
</html>
